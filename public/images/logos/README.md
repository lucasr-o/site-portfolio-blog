# Institution logos (Background marquee)

Drop transparent-background PNG/SVG logos here. They render as a monochrome
"logo wall" (uniform light silhouette, original color on hover). Missing files
fall back to the text label automatically.

Expected filenames (referenced in lib/data.ts):
- pagbank.png
- ufabc.png
- cisco.png
- senai.png    (use a transparent version — not the red-block one)

Optional (currently shown as text):
- go-ahead.png, isc2.png, cambridge.png  (add + wire in lib/data.ts to use)
